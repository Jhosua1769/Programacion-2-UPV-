
// let array = [1, 2, 3]


word = "ase", "pdf";

word.split("");

let array = [1, 2, 3];

    // let accu = 0;
    
    //force cimpile

    // for (let i = 0; i < array.length; i++) {
        
    //     //element = array[i];

    //     accu = accu + array[i];
        
    // }


    let count = array.reduce((accu, element, index) => {
        accu = accu + element;

        return accu;
    }, 0);


    console.log(count);






function (params) {
    words = element.split("")
}
